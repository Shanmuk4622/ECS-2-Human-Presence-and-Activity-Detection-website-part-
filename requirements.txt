# ECS-2 Python pipeline dependencies
# This repo's web UI is static HTML/CSS/JS and does not require Python to run.
# Install these only if you are working on CSI processing and model training.

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# Deep learning stack (training/inference)
torch>=2.2
torchvision>=0.17

# Data acquisition and image pipeline
pyserial>=3.5
opencv-python>=4.9
Pillow>=10.0

# Notebook and experiment tracking (optional)
jupyter>=1.0
